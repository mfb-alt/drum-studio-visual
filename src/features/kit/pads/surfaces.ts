/** Shared material recipes so every pad component looks like the same kit. */
export const RUBBER_SURFACE =
  "radial-gradient(ellipse at 42% 20%, var(--pad-rubber-hi), var(--pad-rubber) 58%, var(--pad-shell) 100%)";

export const METAL_SURFACE =
  "linear-gradient(160deg, var(--metal-hi), var(--metal) 45%, var(--metal-dark))";

export const DROP_SHADOW = "0 18px 30px -18px oklch(0 0 0 / 0.95)";